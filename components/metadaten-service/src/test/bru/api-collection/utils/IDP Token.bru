meta {
  name: IDP Token
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/token
  body: none
  auth: none
}

vars:post-response {
  dynamic_jwt: res.body
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
